# Pages directory

This directory contains the top-level page components for the app:
- `HomePage.jsx`  – The main page with the map and sidebar.
- `AddSpacePage.jsx` – The page wrapping the add form.
- `DetailsPage.jsx` – The full park detail view.
